<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <style>

      label{
        font-size: 20px;
      }

      a{
        color: yellow;
      }

      body{
        background-color: #55423d;
        color: white;
      }
      
      .property-map {
        border: thin black solid;
      }

       .entire_page {
        display: flex;
        flex-direction: column;
        gap: 15px;
        justify-content: center;
        flex-grow: 1;
      }

      .title {
        color: #ffc6c7;
        display: flex;
        justify-content: center;
      }

      .form{
        display: flex;
        justify-content: center;
      }

      .table{
        display: flex;
        justify-content: center;
      }

      .button{
        color: #00473e;
        font-size: 14px;
      }

      .header{
        color: white;
      }

      .text{
        font-size: 20px;
      }

      .text_2{
        font-size: 18px;
      }

      .text_color{
        color: #ffc6c7;
      }

    </style>

  </head>

  <body>
    <nav>
      <div class="text"><a href="/">Home</a></div>
      <div class="text"><a href="/stands">All Stands</a></div>
      <div class="text"><a href="/winds">Choosing a stand to hunt based on wind</a></div>
      <div class="text"><a href="/crops">Choosing a stand to hunt based on crops</a></div>
      <hr>
    </nav>
    <%= yield %>
  </body>
</html>
